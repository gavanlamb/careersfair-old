@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@section stylesheet{
    @Styles.Render("~/Content/Bootstrap")
    @Styles.Render("~/Content/Site")
    @RenderSection("stylesheet", required: false)
}
@section scripts{
    @Scripts.Render("~/Bundles/Bootstrap")
    @RenderSection("scripts", required: false)
}
<nav class="navbar navbar-default">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="container">
        <div class="navbar-header">
            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
                <span class="sr-only">Toggle navigation</span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
                <span class="icon-bar"></span>
            </button>
            @Html.ActionLink("Careers Fair", "CreateForm", null, new { @class = "navbar-brand" })
        </div>

        <!-- Collect the nav links, forms, and other content for toggling -->
        <!--<div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <div class="nav navbar-nav navbar-right">
                <ul class="nav navbar-nav navbar-right">
                    <li>@Html.ActionLink("Home", "CreateForm")</li>
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false">Form <span class="caret"></span></a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("All Forms", "Index")</li>
                            <li>@Html.ActionLink("Form Builder", "Create")</li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>-->
    </div>
</nav>

<main class="container">
    @RenderBody()
</main>

<footer class="navbar navbar-fixed-bottom">
    <ul class="list-unstyled container text-center"><li><p>&copy; @DateTime.Now.Year - Fiserv</p></li></ul>
</footer>